name: One-off Policy HTTP Go Format

on:
  push:
    branches:
      - feat/policy-version-activation-v2
    paths:
      - .github/workflows/one-off-policy-http-gofmt.yml

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feat/policy-version-activation-v2
      - uses: actions/setup-go@v7
        with:
          go-version: '1.26.5'
          cache: false
      - name: Format policy HTTP files
        shell: bash
        run: |
          set -euo pipefail
          gofmt -w internal/httpapi/policy_mutations.go internal/httpapi/policy_mutations_test.go apps/control-plane/main.go
          if git diff --quiet -- internal/httpapi/policy_mutations.go internal/httpapi/policy_mutations_test.go apps/control-plane/main.go; then
            echo 'Policy HTTP files already formatted.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add internal/httpapi/policy_mutations.go internal/httpapi/policy_mutations_test.go apps/control-plane/main.go
          git commit -m 'style: format policy HTTP code'
          git push origin HEAD:feat/policy-version-activation-v2
